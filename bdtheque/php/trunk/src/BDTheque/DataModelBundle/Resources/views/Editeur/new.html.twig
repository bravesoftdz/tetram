{% extends "BDThequeDataModelBundle::editForm.layout.html.twig" %}

{% block contentPanel %}
<h1>Editeur creation</h1>

<form action="{{ path('editeur_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
        <p>
            <button type="submit" class="uibutton">Create</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('editeur') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
