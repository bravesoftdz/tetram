{% extends "BDThequeDataModelBundle::edit.layout.html.twig" %}

{% block content %}
<h1>Genre creation</h1>

<form action="{{ path('genre_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
        <p>
            <button type="submit" class="uibutton">Create</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('genre') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
