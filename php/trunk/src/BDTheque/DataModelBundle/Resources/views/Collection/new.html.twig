{% extends "BDThequeDataModelBundle::edit.layout.html.twig" %}

{% block content %}
<h1>Collection creation</h1>

<form action="{{ path('collection_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
        <p>
            <button type="submit" class="uibutton">Create</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('collection') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
